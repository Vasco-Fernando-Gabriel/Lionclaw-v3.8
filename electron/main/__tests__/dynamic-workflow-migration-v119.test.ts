
import { describe, it, expect } from 'vitest';

describe('migration v119 (obsoleta: colapsada pela V120 modo unico)', () => {
  it('movida para dynamic-workflow-migration-v120.test.ts', () => {
    expect(true).toBe(true);
  });
});
